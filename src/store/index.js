import Vue from "vue";
import Vuex from "vuex";
import collection from './collection'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    collection
  },
});
